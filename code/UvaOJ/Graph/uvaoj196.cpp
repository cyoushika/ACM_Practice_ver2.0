#include<stdio.h>
#include<string.h>
#define MAX_R 1010
#define MAX_C 18300
#define MAXN 50000

struct Cell{
    char str[MAXN];
};

char map[MAX_R][MAX_C];
int row, col;

int main(){
    int T;
    scanf("%d",&T);
    while(T--){
        scanf("%d%d",&col,&row);
    }
}
